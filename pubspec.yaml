name: "flutter_validator_pro"
description: "A Flutter plugin for form validation including email, password, phone number, and custom multi-validator support."
version: 0.1.3
homepage: "https://github.com/Bharat9011/flutter_validation"

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter: